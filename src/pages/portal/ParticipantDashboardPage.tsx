import { mockAnnouncements, mockParticipant, mockProgramme, participantMetrics } from '../../data/mockPlatform'
import { DashboardCard } from '../../components/platform/DashboardCard'
import { PageHeader } from '../../components/platform/PageHeader'
import { StatusBadge } from '../../components/platform/StatusBadge'

export function ParticipantDashboardPage() {
  return <div className="mx-auto max-w-6xl"><PageHeader eyebrow="MKAPS 2027" title={`Welcome, ${mockParticipant.name.split(' ')[0]}`}><div className="text-sm"><p className="text-slate-500">Registration number</p><p className="mt-1 font-bold">{mockParticipant.registrationNumber}</p></div></PageHeader><div className="mt-7 flex flex-wrap items-center gap-3 border border-mkaps-gold/30 bg-amber-50 p-4"><strong className="text-sm">Your registration is ready.</strong><StatusBadge status={mockParticipant.registrationStatus} /><p className="w-full text-xs text-slate-600 sm:ml-auto sm:w-auto">Review your outstanding balance before arrival.</p></div><section aria-labelledby="status-title" className="mt-8"><h2 id="status-title" className="text-sm font-bold uppercase tracking-wider">What you need to know</h2><div className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{participantMetrics.map((metric) => <DashboardCard key={metric.label} {...metric} />)}</div></section><div className="mt-8 grid gap-5 lg:grid-cols-2"><article className="border border-slate-200 bg-white p-6"><p className="text-[.65rem] font-bold uppercase tracking-wider text-mkaps-gold">Next programme session</p><h2 className="mt-3 font-display text-2xl font-semibold">{mockProgramme[0].title}</h2><p className="mt-3 text-sm text-slate-500">{mockProgramme[0].time} · {mockProgramme[0].venue}</p></article><article className="border border-slate-200 bg-white p-6"><p className="text-[.65rem] font-bold uppercase tracking-wider text-mkaps-gold">Latest announcement</p><h2 className="mt-3 font-display text-2xl font-semibold">{mockAnnouncements[0].title}</h2><p className="mt-3 text-sm leading-6 text-slate-500">{mockAnnouncements[0].message}</p></article></div></div>
}
